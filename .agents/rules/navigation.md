# Navigation & Link Component Rule

- Always use Next.js `Link` component (`import Link from "next/link"`) instead of HTML `<a>` tags for navigation across the application.
- Ensure proper accessibility attributes and styling are maintained on the `Link` components.
